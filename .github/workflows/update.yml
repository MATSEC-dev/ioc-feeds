name: Update OpenPhish Feed

on:
  workflow_dispatch:

  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install requests

      - name: Download feed
        run: python scripts/update_openphish.py

      - name: Update README
        run: python scripts/update_readme.py

      - name: Commit changes
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com

          git add feeds/openphish.txt README.md

          git diff --staged --quiet || git commit -m "Update OpenPhish feed"

          git push
